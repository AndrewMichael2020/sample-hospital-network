# MySQL Database Configuration
# Update these values for your local development environment

# MySQL Connection Settings
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=lm_synth

# For Docker Compose / GitHub Codespaces
# Uncomment and set these if using containerized environment
# MYSQL_ROOT_PASSWORD=devpassword123
# MYSQL_USER=hospital_user
# MYSQL_PASSWORD=devpassword

# Data Generation Settings
# Number of synthetic patients to generate (default: 1000)
# PATIENTS_COUNT=1000

# Random seed for reproducible data generation
# DATA_SEED=42

# API Server Settings (for local development)
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=true

# Data Directory (where CSV files are stored)
DATA_DIR=./data